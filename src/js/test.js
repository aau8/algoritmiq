const price = 100; // Цена товара
const sale = 10; // Скидка
const quantity = 5; // Кол-во товара

// INFO: Цена с учетом скидки
function priceDiscount(price, sale) {
    return price * sale / 100;
}

// TODO: Написать функцию вывода общей цены
// function totalPrice(price, quantity) {

// }

// FIX: Исправить ошибку в функции
function localPrice(price) {
    // retarn price.toLocaleString('ru-RU', { style: 'currency', currency: 'RUB' })
}

methodName(params) {
    
}

get propertyName() {
    return this.;
}

pse

console.log(object);